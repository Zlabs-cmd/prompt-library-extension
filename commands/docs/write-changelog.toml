prompt = """
# Generate Changelog

Create a structured changelog entry for the following updates. Use the "Keep a Changelog" format with categories like:
- Added (for new features)
- Changed (for changes in existing functionality)
- Deprecated (for soon-to-be removed features)
- Removed (for now removed features)
- Fixed (for any bug fixes)
- Security (in case of vulnerabilities)

Updates/Commits:
{{args}}

Organize the entries by version and date if provided.
"""
